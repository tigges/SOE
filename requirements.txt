# SOE toolkit runtime dependencies.
# Core crawl/parse/config stack used by every soe_*.py script.
requests>=2.32,<3
beautifulsoup4>=4.12,<5
lxml>=5.1,<7
PyYAML>=6.0,<7

# Optional: unlocks the Google Search Console / Business Profile integrations
# (integrations/gsc.py, integrations/gbp.py). Safe to keep installed even when
# no Google keys are configured — the modules skip cleanly without credentials.
google-auth>=2.30,<3
